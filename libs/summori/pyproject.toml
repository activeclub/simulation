[project]
name = "summori"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["mypy==1.13.0", "ruff==0.8.2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
